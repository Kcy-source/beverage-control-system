// 销售提成：同一员工、同一日期可一次录入多种饮料。
(function(){
'use strict';let installed=false,entryLines=[];
function todaySG(){return new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Singapore'});}function recordDate(r){return r.operation_date||new Date(r.created_at).toLocaleDateString('en-CA',{timeZone:'Asia/Singapore'});}
function wait(){if(typeof sb==='undefined'||typeof items==='undefined'||typeof commissions==='undefined'||typeof render!=='function'||!document.getElementById('commission')||!document.getElementById('itemOperationDate')){setTimeout(wait,150);return;}install();}
function install(){if(installed)return;installed=true;const originalOpenStock=openStock;openStock=function(id,action){originalOpenStock(id,action);const w=document.getElementById('sellerWrap');if(w){w.classList.add('hidden');w.style.display='none';}};build();const old=render;render=function(){old();refresh();};refresh();}
function build(){const section=document.getElementById('commission');if(document.getElementById('manualCommissionPanel'))document.getElementById('manualCommissionPanel').remove();const p=document.createElement('div');p.id='manualCommissionPanel';p.style.cssText='border:1px solid #e5e7eb;border-radius:13px;padding:16px;margin-bottom:16px;background:#fafbfc';p.innerHTML=`<div class="section-head"><h2 style="font-size:18px">销售提成录入</h2></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px"><label>日期<input id="manualCommissionDate" type="date"></label><label>员工<input id="manualCommissionSeller" list="manualCommissionSellerList" placeholder="员工姓名"><datalist id="manualCommissionSellerList"></datalist></label></div><div style="border:1px solid #e5e7eb;border-radius:10px;padding:12px;background:#fff"><div style="display:grid;grid-template-columns:minmax(220px,2fr) 110px 130px auto;gap:8px;align-items:end"><label>饮料<select id="manualCommissionItem"></select></label><label>数量<input id="manualCommissionQty" type="number" min="0.01" step="0.01"></label><label>单位提成 SGD<input id="manualCommissionRate" type="number" min="0" step="0.01"></label><button id="addCommissionLineBtn" class="secondary">＋ 添加</button></div><div id="commissionEntryLines" style="margin-top:10px"></div></div><label style="display:block;margin-top:10px">备注<input id="manualCommissionNote" placeholder="备注（选填）"></label><div style="display:flex;justify-content:flex-end;margin-top:12px"><button id="saveManualCommissionBtn">保存</button></div><div id="manualCommissionMessage" class="hint" style="margin-top:8px"></div>`;section.insertBefore(p,section.firstChild);document.getElementById('manualCommissionDate').value=todaySG();document.getElementById('manualCommissionItem').onchange=fillRate;document.getElementById('addCommissionLineBtn').onclick=addLine;document.getElementById('saveManualCommissionBtn').onclick=saveAll;
const filter=document.getElementById('manualCommissionFilterPanel');if(filter)filter.remove();const f=document.createElement('div');f.id='manualCommissionFilterPanel';f.style.cssText='display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px;padding:12px;margin:0 0 14px;border:1px solid #e5e7eb;border-radius:12px;background:#fff';f.innerHTML=`<label>员工<select id="commissionSellerFilter"><option value="">全部员工</option></select></label><label>饮料<select id="commissionItemFilter"><option value="">全部饮料</option></select></label><label>开始日期<input id="commissionDateFrom" type="date"></label><label>结束日期<input id="commissionDateTo" type="date"></label><div style="display:flex;align-items:flex-end"><button id="clearCommissionFiltersBtn" class="secondary" style="width:100%">重置</button></div>`;const table=section.querySelector('table');section.insertBefore(f,table);let summary=document.getElementById('manualCommissionSummary');if(summary)summary.remove();summary=document.createElement('div');summary.id='manualCommissionSummary';summary.className='hint';summary.style.marginBottom='10px';section.insertBefore(summary,table);const h=section.querySelector(':scope>.section-head h2');if(h)h.textContent='销售提成记录';const hr=table.querySelector('thead tr');if(hr)hr.innerHTML='<th>日期</th><th>员工</th><th>饮料</th><th>数量</th><th>单位提成</th><th>提成金额</th><th>备注</th><th>录入账号</th><th>操作</th>';['commissionSellerFilter','commissionItemFilter','commissionDateFrom','commissionDateTo'].forEach(id=>document.getElementById(id).onchange=renderRows);document.getElementById('clearCommissionFiltersBtn').onclick=()=>{['commissionSellerFilter','commissionItemFilter','commissionDateFrom','commissionDateTo'].forEach(id=>document.getElementById(id).value='');renderRows();};}
function refresh(){const entry=document.getElementById('manualCommissionItem'),filter=document.getElementById('commissionItemFilter');if(!entry||!filter)return;const ev=entry.value,fv=filter.value,sorted=[...items].sort((a,b)=>String(a.name).localeCompare(String(b.name)));entry.innerHTML='<option value="">请选择饮料</option>'+sorted.map(x=>`<option value="${x.id}">${esc(x.name)}${x.spec?' · '+esc(x.spec):''}</option>`).join('');const names=[...new Set([...sorted.map(x=>x.name),...commissions.map(x=>x.item_name).filter(Boolean)])].sort();filter.innerHTML='<option value="">全部饮料</option>'+names.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');if([...entry.options].some(o=>o.value===ev))entry.value=ev;if([...filter.options].some(o=>o.value===fv))filter.value=fv;const sellers=[...new Set(commissions.map(x=>x.seller_name).filter(Boolean))].sort();document.getElementById('manualCommissionSellerList').innerHTML=sellers.map(x=>`<option value="${esc(x)}"></option>`).join('');const sf=document.getElementById('commissionSellerFilter'),sv=sf.value;sf.innerHTML='<option value="">全部员工</option>'+sellers.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');if([...sf.options].some(o=>o.value===sv))sf.value=sv;renderLines();renderRows();}
function fillRate(){const x=items.find(i=>i.id===document.getElementById('manualCommissionItem').value);document.getElementById('manualCommissionRate').value=x?Number(x.commission_per_unit||0).toFixed(2):'0.00';}
function addLine(){const id=document.getElementById('manualCommissionItem').value,x=items.find(i=>i.id===id),qty=Number(document.getElementById('manualCommissionQty').value),rate=Number(document.getElementById('manualCommissionRate').value||0);if(!x)return alert('请选择饮料');if(!Number.isFinite(qty)||qty<=0)return alert('请输入有效数量');if(!Number.isFinite(rate)||rate<0)return alert('请输入有效提成');entryLines.push({item_id:x.id,item_name:x.name,quantity:qty,rate});document.getElementById('manualCommissionItem').value='';document.getElementById('manualCommissionQty').value='';document.getElementById('manualCommissionRate').value='';renderLines();}
function renderLines(){const box=document.getElementById('commissionEntryLines');if(!box)return;box.innerHTML=entryLines.map((x,i)=>`<div style="display:grid;grid-template-columns:1fr 80px 100px 90px 60px;gap:8px;align-items:center;padding:8px 0;border-top:1px solid #eee"><b>${esc(x.item_name)}</b><span>× ${x.quantity}</span><span>$${Number(x.rate).toFixed(2)}</span><span class="money">$${(x.quantity*x.rate).toFixed(2)}</span><button class="red" style="padding:5px" onclick="window.removeCommissionEntryLine(${i})">删除</button></div>`).join('')||'<div class="hint">尚未添加饮料</div>';}
async function saveAll(){const date=document.getElementById('manualCommissionDate').value,seller=document.getElementById('manualCommissionSeller').value.trim(),note=document.getElementById('manualCommissionNote').value.trim(),msg=document.getElementById('manualCommissionMessage');if(!date)return alert('请选择日期');if(!seller)return alert('请输入员工姓名');if(!entryLines.length)return alert('请至少添加一种饮料');const payload=entryLines.map(x=>({item_id:x.item_id,item_name:x.item_name,seller_name:seller,quantity:x.quantity,commission_per_unit:x.rate,commission_amount:x.quantity*x.rate,operation_date:date,note,user_email:userEmail()}));msg.textContent='保存中...';const {error}=await sb.from('commission_logs').insert(payload);if(error){msg.textContent='';return alert('保存失败：'+error.message);}entryLines=[];document.getElementById('manualCommissionNote').value='';msg.textContent='保存成功';renderLines();await loadAll();}
async function del(id){if(!confirm('确认删除该销售提成记录？'))return;const {error}=await sb.from('commission_logs').delete().eq('id',id);if(error)return alert('删除失败：'+error.message);await loadAll();}
function renderRows(){const body=document.getElementById('commissionBody'),summary=document.getElementById('manualCommissionSummary');if(!body||!summary)return;const seller=document.getElementById('commissionSellerFilter')?.value||'',name=document.getElementById('commissionItemFilter')?.value||'',from=document.getElementById('commissionDateFrom')?.value||'',to=document.getElementById('commissionDateTo')?.value||'';const rows=[...commissions].filter(r=>{const d=recordDate(r);return(!seller||r.seller_name===seller)&&(!name||r.item_name===name)&&(!from||d>=from)&&(!to||d<=to);}).sort((a,b)=>recordDate(b).localeCompare(recordDate(a)));summary.textContent=`记录 ${rows.length}｜销售数量 ${rows.reduce((s,r)=>s+Number(r.quantity||0),0)}｜提成 $${rows.reduce((s,r)=>s+Number(r.commission_amount||0),0).toFixed(2)}`;body.innerHTML=rows.map(r=>`<tr><td>${esc(recordDate(r))}</td><td><b>${esc(r.seller_name||'')}</b></td><td>${esc(r.item_name||'')}</td><td>${Number(r.quantity||0)}</td><td>$${Number(r.commission_per_unit||0).toFixed(2)}</td><td class="money">$${Number(r.commission_amount||0).toFixed(2)}</td><td>${esc(r.note||'')}</td><td>${esc(r.user_email||'')}</td><td><button class="red" onclick="window.deleteCommissionRecord('${r.id}')">删除</button></td></tr>`).join('')||'<tr><td colspan="9">暂无销售记录</td></tr>';}
window.removeCommissionEntryLine=i=>{entryLines.splice(i,1);renderLines();};window.deleteCommissionRecord=del;wait();
})();